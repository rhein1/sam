version: "v1alpha1"
attenuation:
  # Allow cross-node MCP only when the caller's biscuit carries a
  # node($peer) fact — i.e. it was issued by our control plane upon enrollment.
  policies:
services:
  - type: "mcp"
    name: "calculator"
    description: "Simple math operations"
    target_url: "http://127.0.0.1:7777/mcp"
